import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
// root contains our rendered site
const root = ReactDOM.createRoot(document.getElementById('root')); // creating root container
root.render(
  <App/>
  // <h1>Hello World--hlo hlo</h1>
);